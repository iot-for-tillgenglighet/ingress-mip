package se.masarinconsulting.iothub;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeIngressMIPIT extends IngressMIPTest {

    // Execute the same tests but in native mode.
}